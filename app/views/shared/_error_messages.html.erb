<% if @user.errors.any? %>
  <p>The form contains <%= pluralize(@user.errors.count, "error") %> </p>
  <ul class="alert-form">
    <% @user.errors.full_messages.each do |msg| %>
  <li>* <%= msg %> </li>
<% end %>
</ul>
<% end %>
